services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pricehub
      POSTGRES_PASSWORD: pricehub
      POSTGRES_DB: price_hub
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pricehub -d price_hub"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
